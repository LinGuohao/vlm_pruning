name: vlm_pruning
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  # Python & base
  - python=3.10
  - pip
  - git

  # GPU PyTorch (CUDA 12.1)
  - pytorch=2.4.1
  - torchvision=0.19.1
  - torchaudio=2.4.1
  - pytorch-cuda=12.1  

  # 工具
  - uv 
